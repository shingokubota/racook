<header id="fh5co-header-section">
  <div class="container">
    <div class="nav-header">
      <%# <a data-turbolinks="false" href="#" class="js-fh5co-nav-toggle fh5co-nav-toggle"><i></i></a> %>
      <h1 id="fh5co-logo">
        <%= link_to(image_tag("logo.jpg", :alt => "ラクックイメージ"), root_url, id: 'logo') %>
      </h1>
      <!-- START #fh5co-menu-wrap -->
      <nav class="phone_area">
        <ul class="sf-menu" id="fh5co-primary-menu">
          <% if logged_in? %>
            <% if current_user.notification == true %>
              <li class="new_notification header-link">
            <% else %>
              <li class="no_notification header-link">
            <% end %>
            <%= link_to notifications_path do %><span class="fas fa-bell"></span><span> 通知</span>
            <% if current_user.notification_count != 0 %>
            <span class="notification_count"><%= current_user.notification_count %><span>
            <% end %></span><% end %>
            </li>
            <li>
            <%= link_to favorites_path do %><span class="fas fa-heart"></span><span> お気に入り</span><% end %>
            </li>
            <li>
            <%= link_to 'ユーザー一覧', users_path %>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">アカウント<b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to "プロフィール", current_user %></li>
                <li><%= link_to "ログアウト", logout_path, method: :delete %></li>
              </ul>
            </li>
            <li>
            <%= link_to(new_dish_path, class:"recipi") do  %>
            <span class="btn btn-primary btn-lg btn-record">レシピ登録</span>
            <% end %>
            </li>
          <% else %>
            <li>
            <%= link_to about_path do %><span class="fas fa-question-circle"></span><span> ラクックとは</span><% end %>
            </li>
            <li>
            <%= link_to signup_path do %><span class="fas fa-user-plus"></span><span> ユーザー登録（無料）</span><% end %>
            </li>
            <li class="header-link">
            <%= link_to 'ログイン', login_path %>
            </li>
          <% end %>
        </ul>
      </nav>
      <nav class="pc_area" id="fh5co-menu-wrap" role="navigation">
        <ul class="sf-menu" id="fh5co-primary-menu">
          <% if logged_in? %>
            <% if current_user.notification == true %>
              <li class="new_notification header-link">
            <% else %>
              <li class="no_notification header-link">
            <% end %>
            <%= link_to notifications_path do %><span class="fas fa-bell"></span><span> 通知</span>
            <% if current_user.notification_count != 0 %>
            <span class="notification_count"><%= current_user.notification_count %><span>
            <% end %></span><% end %>
            </li>
            <li>
            <%= link_to favorites_path do %><span class="fas fa-heart"></span><span> お気に入り</span><% end %>
            </li>
            <li>
            <%= link_to 'ユーザー一覧', users_path %>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">アカウント<b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><%= link_to "プロフィール", current_user %></li>
                <li><%= link_to "ログアウト", logout_path, method: :delete %></li>
              </ul>
            </li>
            <li>
            <%= link_to(new_dish_path, class:"recipi") do  %>
            <span class="btn btn-primary btn-lg btn-record">レシピ登録</span>
            <% end %>
            </li>
          <% else %>
            <li>
            <%= link_to about_path do %><span class="fas fa-question-circle"></span><span> ラクックとは</span><% end %>
            </li>
            <li>
            <%= link_to signup_path do %><span class="fas fa-user-plus"></span><span> ユーザー登録（無料）</span><% end %>
            </li>
            <li class="header-link">
            <%= link_to 'ログイン', login_path %>
            </li>
          <% end %>
        </ul>    
      </nav>
    </div>
  </div>
</header>

